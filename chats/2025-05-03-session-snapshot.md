# Session Snapshot [2025-05-03]

## What We’ve Done
- Initialized the GitHub repository `cmms-project`.
- Created top-level folders: `docs/`, `code/`, and `chats/`.
- Added `session-snapshot-template.md` in `chats/`.
- Created the first dated snapshot file `2025-05-03-session-snapshot.md`.

## Decisions Made
- We will use GitHub for version control of all project artifacts.
- Folder structure will be `docs/` for documentation, `code/` for code, and `chats/` for session snapshots.
- Every ChatGPT session will generate a dated snapshot in `chats/` to preserve context.

## Outstanding Questions / Next Steps
- Draft the **Project Charter** document in `docs/`.
- Define the **Asset Registry** fields as part of the FRD under `docs/`.
- Set up a simple local “hello world” in `code/` to verify the dev environment.